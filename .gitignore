*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# Committed on purpose, not ignored: this host's deploy is a plain git
# pull straight into public_html with no build step available (no
# Node/npm on the server), so the compiled assets have to travel in git
# itself or the site has nothing to serve. Rebuild locally with
# `npm run build` and commit the result whenever resources/ changes.
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
